# Ralphy Progress Log
Started: воскресенье, 15 марта 2026 г. 19:46:39 (EET)
---

## Codebase Patterns
- Build uses `tsup` with multi-config array in `tsup.config.ts` (ESM, CJS, IIFE browser bundles, CLI)
- CLI is built by tsup as ESM bundle in `dist/cli/` — shebang is preserved from source `cmd/cli.ts` (do NOT add a banner shebang or it will be duplicated)
- `yargs/helpers` and `yargs/yargs` must be listed as external in tsup config (they are separate entry points, not subpaths of `yargs`)
- `exports` map: `types` condition must come FIRST before `import`/`require` for TypeScript resolution to work correctly
- `wampcra.js` CJS require path points to `.mjs` file (top-level await incompatible with CJS)
- Post-build script `scripts/post-build.js` creates `dist/browser.zip` using system `zip` command
- `wampy-cra.js` uses top-level `await import('node:crypto')` — incompatible with CJS format, so CJS build emits it as `.mjs`
- Typecheck script uses `--declaration false --declarationMap false` flags with `--noEmit` to avoid TS9005 errors from JS test files
- `checkJs: false` in tsconfig.json — JS files are included for module resolution but not type-checked
- External deps for library builds: cbor-x, msgpackr, tweetnacl, websocket, ws, yargs, color-json, node:crypto
- Browser IIFE bundles use `noExternal: [/.*/]` to bundle all dependencies inline
- When renaming `.js` → `.ts`: import paths in consuming files stay as `.js` (NodeNext resolution), but `tsup.config.ts` entry paths must be updated to `.ts`
- For globalThis augmentations (e.g., `MozWebSocket`), use `declare global { var ... }` — NOT `declare const globalThis: typeof globalThis & { ... }` (causes circular ref)
- Serializer interface defined in `src/serializers/serializer.ts` — all serializers implement it; import as `import type { Serializer } from './serializer.js'`
- When third-party types are incomplete (e.g., cbor-x missing `int64AsNumber`), extend the interface locally rather than using `@ts-ignore`
- `node:crypto` SubtleCrypto and DOM SubtleCrypto types are incompatible — extract `subtle` property directly and type-assert to `SubtleCrypto` to unify both environments
- `tweetnacl` ships its own `.d.ts` — no `@types/tweetnacl` needed
- Core Wampy types are in `src/types.ts` — import with `import type { ... } from './types.js'`
- `Deferred<T>` is in `src/utils.ts` — used by cache (connectPromise, closePromise) and internal requests/calls
- `WampyCache.timer` is typed as `ReturnType<typeof setTimeout> | null` for cross-environment compatibility
- Internal `_requests` map uses `WampRequest` type; `_calls` map uses `WampCall` type (extends Deferred with optional `onProgress`)
- Subscribe/Register request callbacks extend `Deferred` with additional properties (`onEvent` for subscribe, `rpc` for register)
- `Deferred<T>` is contravariant on `onSuccess` — when storing typed Deferreds in `WampRequest.callbacks` (which expects `Deferred<unknown>`), cast via `as unknown as Deferred`
- `SUCCESS` constant from constants.ts lacks `reqId` — use `{ ...SUCCESS, reqId: 0 }` when assigning to `WampyOpStatus`
- When passing `this._options` to `getWebSocket()`, cast as `Record<string, unknown>` to avoid `null` vs `undefined` incompatibility on `ws` property
- `_isPlainObject` is typed as a type guard: `input is Record<string, unknown>` — this narrows types in conditional blocks
- CLI files (`cmd/`) are NOT built by tsup — they run from source. `bin` entry in package.json points to `cmd/cli.js`
- yargs `MiddlewareFunction` expects `void` return — middleware callbacks that mutate `argv` should NOT return the argv object
- Auth plugin `sign()` returns functions with specific info types (`WampCraInfo`, `CryptosignInfo`) — cast to `AuthPlugin` when assigning to `authPlugins`
- `ws` module needs `@types/ws` for TypeScript; `yargs` needs `@types/yargs`
- yargs `CommandModule` handler must accept `Arguments` — cast to specific argv interface inside the handler body, not in the parameter type
- CLI command interfaces extend `CliArgv` (from `wampy-helpers.ts`) with command-specific options (positionals, flags)
- Test fake WS files use function-constructor pattern — type with a `FakeWebSocket` interface and cast `function() {} as unknown as FakeWebSocketConstructor`
- `WAMP_MSG_SPEC` is `as const` — its values are literal types. When using `.includes()`, cast the array to `number[]` (e.g., `([WAMP_MSG_SPEC.CALL, ...] as number[]).includes(value)`)
- `send-data.ts` exports `SendDataItem` interface for typing the mock server response data
- `@types/mocha`, `@types/chai`, `@types/sinon` are added as devDependencies for test file typing
- `@types/websocket` added for crossbar test typing
- `globalThis.location` in tests: DOM `Location` type conflicts with partial mock objects — use `const g = globalThis as Record<string, unknown>` alias
- `globalThis = savedGlobalThis` reassignment is not allowed in strict TS (namespace) — use `@ts-expect-error` comment
- `chai-bytes` has bundled types (`index.d.ts`) — no `@types/chai-bytes` needed
- For test files accessing globalThis properties that may/may not exist (Wampy, MsgpackSerializer, etc.), cast `globalThis as Record<string, unknown>` to avoid type errors
- Test runner uses `tsx` via `NODE_OPTIONS='--import tsx/esm'` for ESM + TypeScript mocha support — mocha does NOT have a `--import` flag, so `NODE_OPTIONS` is required
- Karma browser tests use `karma-esbuild` preprocessor (replaces `karma-browserify` + `babelify`) — esbuild handles TypeScript natively with no extra config
- c8 coverage config `.c8rc.json` include patterns must match source file extensions (`.ts` not `.js`)
- ESLint uses `typescript-eslint` v8 with flat config via `tseslint.config()` wrapper
- TS-specific ESLint rule overrides must come AFTER base rules (flat config applies rules in order, last wins)
- `no-duplicate-imports` must be disabled for `.ts` files — `import type` + regular `import` from same module is valid TS
- `no-unused-vars` / `no-use-before-define` / `default-param-last` have TS equivalents — disable base rule and enable `@typescript-eslint/` version
- `@typescript-eslint/no-unused-expressions` must be disabled for test files — chai assertion expressions (`expect().to.be.true`) trigger it
- `@typescript-eslint/no-this-alias` must be disabled when using `consistent-this` pattern (`const self = this`)
- `eslint --fix` auto-removes unused eslint-disable directives when `reportUnusedDisableDirectives: 'warn'` is set

## 2026-03-15 20:00 (EET) - US-001
- Implemented TypeScript infrastructure and tsup build configuration
- Files changed:
  - Created: `tsconfig.json`, `tsup.config.ts`, `scripts/post-build.js`
  - Modified: `package.json` (new scripts, new devDeps, removed Babel/Grunt deps)
  - Deleted: `.babelrc`, `Gruntfile.cjs`
  - Modified: `package-lock.json` (dependency tree updated)
- **Learnings for future iterations:**
  - tsup config supports array of configs for multiple output formats
  - Top-level `await` in source files (wampy-cra.js line 10) breaks CJS builds — need special handling
  - TS9005 "Declaration emit for this file requires using private name" occurs when `declaration: true` + `allowJs: true` with function-constructor-style exports in test JS files
  - The original Grunt build: babel(src→dist CJS), browserify(browser entries), uglify, concat(serializers+wampy min→wampy-all.min), compress(zip), clean(browser folder)
  - Browser entries (wampy-single-4-browser.js, wampy-all-4-browser.js) set `globalThis.*` directly — the IIFE wrapper's globalName is unused
  - `wampy-all-4-browser.js` explicitly excludes wampcra due to `await import('node:crypto')` incompatibility
---

## 2026-03-15 20:10 (EET) - US-002
- Converted `src/constants.js` → `src/constants.ts` with proper types: `WAMP_MSG_SPEC` as const object, `WAMP_ERROR_MSG` as `Record<string, string>`, `SUCCESS` typed with readonly literal types, `E2EE_SERIALIZERS` as `readonly string[]`, `isNode` as boolean, `WAMP_CUSTOM_ATTR_REGEX` as RegExp
- Converted `src/errors.js` → `src/errors.ts` with all error classes typed: readonly `code` properties, typed constructor parameters, `WampErrorParams` interface, `override` keyword on subclass `code` properties
- Updated `tsup.config.ts` entry paths from `.js` to `.ts` for constants and errors
- Files changed:
  - Created: `src/constants.ts`, `src/errors.ts`
  - Deleted: `src/constants.js`, `src/errors.js`
  - Modified: `tsup.config.ts` (entry paths updated)
- **Learnings for future iterations:**
  - When renaming `.js` → `.ts`, import paths with `.js` extensions in consuming files DON'T need updating — NodeNext module resolution resolves `.js` imports to `.ts` source files
  - `tsup.config.ts` entry paths DO need updating from `.js` to `.ts` since those are direct file paths, not module specifiers
  - `E2EE_SERIALIZERS` is used with `.includes()` in wampy.js — can't convert to `Set` until wampy.js is also converted (Set uses `.has()`)
  - Error subclasses overriding a readonly property from parent need the `override` keyword in strict mode
  - tsup generates chunked `.d.ts` files (e.g., `errors-C_o2yEYd.d.ts`) and re-exports from the main `.d.ts`
---

## 2026-03-15 20:20 (EET) - US-003
- Converted `src/utils.js` → `src/utils.ts` with proper types
- `getWebSocket()` return type defined as `WebSocket | null`, config parameter typed via `GetWebSocketConfig` interface
- `getNewPromise<T>()` is generic returning `Deferred<T>` with typed `promise`, `onSuccess`, `onError` properties
- Exported `Deferred<T>` interface for use by other modules
- `MozWebSocket` typed via `declare global` augmentation
- Internal helpers (`isWebSocketSchemeSpecified`, `getServerUrlForNode`, `getServerUrlForBrowser`, `getWebSocketFromWindowObject`) all typed with parameter and return types
- JSDoc type annotations removed; textual descriptions preserved
- Updated `tsup.config.ts` entry paths from `.js` to `.ts` for utils
- Files changed:
  - Created: `src/utils.ts`
  - Deleted: `src/utils.js`
  - Modified: `tsup.config.ts` (utils entry paths updated)
- **Learnings for future iterations:**
  - Don't use `declare const globalThis: typeof globalThis & { ... }` — causes TS circular reference error. Use `declare global { var ... }` instead
  - `ws` constructor in user-provided WebSocket class takes 5 args: `(url, protocols, origin, headers, requestOptions)` — the `origin` parameter is `null`
  - The `Deferred<T>` interface will be useful for typing `_requests` and other internal caches in wampy.ts (US-007)
---

## 2026-03-15 21:00 (EET) - US-004
- Created `Serializer` interface in `src/serializers/serializer.ts` with `protocol`, `isBinary`, `encode()`, `decode()` members
- Converted `src/serializers/json-serializer.js` → `src/serializers/json-serializer.ts` implementing `Serializer`
- Converted `src/serializers/cbor-serializer.js` → `src/serializers/cbor-serializer.ts` implementing `Serializer`
- Converted `src/serializers/msgpack-serializer.js` → `src/serializers/msgpack-serializer.ts` implementing `Serializer`
- All three classes implement the `Serializer` interface with proper parameter and return types
- Updated `tsup.config.ts` entry paths from `.js` to `.ts` for all three serializers
- Files changed:
  - Created: `src/serializers/serializer.ts`, `src/serializers/json-serializer.ts`, `src/serializers/cbor-serializer.ts`, `src/serializers/msgpack-serializer.ts`
  - Deleted: `src/serializers/json-serializer.js`, `src/serializers/cbor-serializer.js`, `src/serializers/msgpack-serializer.js`
  - Modified: `tsup.config.ts` (serializer entry paths updated)
- **Learnings for future iterations:**
  - `cbor-x` Options type is missing `int64AsNumber` property — use an extended `CborxOptions` interface to add it
  - `Encoder.encode()` from cbor-x returns `Buffer` (extends `Uint8Array`), compatible with `Serializer` interface's `Uint8Array` return
  - `pack()` from msgpackr returns `Buffer` — also compatible with `Uint8Array` in the `Serializer` interface
  - tsup generates chunked `.d.ts` for shared types (e.g., `serializer-BBXXJ2B8.d.ts`) and re-exports from individual serializer `.d.ts` files
  - The `Serializer` interface is defined in a separate `serializer.ts` file (not an index) to keep it importable as a standalone type
---

## 2026-03-15 21:30 (EET) - US-005
- Converted `src/auth/wampcra/wampy-cra.js` → `src/auth/wampcra/wampy-cra.ts` with full typing
  - `deriveKey()`, `signManual()`, `sign()` all typed with parameter and return types
  - `WampCraInfo` interface defined for the signing info parameter
  - Extracted `subtle` from crypto module with `SubtleCrypto` type assertion to unify Node.js and browser environments
- Converted `src/auth/cryptosign/wampy-cryptosign.js` → `src/auth/cryptosign/wampy-cryptosign.ts` with full typing
  - `hex2bytes()`, `bytes2hex()`, `sign()` all typed with parameter and return types
  - `CryptosignInfo` interface defined for the signing info parameter
  - tweetnacl types used via its bundled `.d.ts`
- Updated `tsup.config.ts` entry paths from `.js` to `.ts` for both auth plugins (ESM, CJS, and CJS/MJS entries)
- JSDoc type annotations (`@param`, `@returns`, `@type`) removed; textual descriptions preserved
- Files changed:
  - Created: `src/auth/wampcra/wampy-cra.ts`, `src/auth/cryptosign/wampy-cryptosign.ts`
  - Deleted: `src/auth/wampcra/wampy-cra.js`, `src/auth/cryptosign/wampy-cryptosign.js`
  - Modified: `tsup.config.ts` (auth entry paths updated)
- **Learnings for future iterations:**
  - `node:crypto` module's `SubtleCrypto` type is NOT compatible with DOM `SubtleCrypto` — the `KeyUsage` types differ (node has extra values like `"decapsulateBits"`)
  - Solution: extract the `subtle` property directly and type-assert it to `SubtleCrypto`, rather than typing the whole crypto module
  - `tweetnacl` ships bundled `nacl.d.ts` — no `@types/tweetnacl` package needed; `sign.detached()` and `sign.keyPair.fromSeed()/fromSecretKey()` are well-typed
   - `str.match(/../g)` can return `null` — need to assert `as RegExpMatchArray` when the input is guaranteed to be valid hex
---

## 2026-03-15 22:00 (EET) - US-006
- Created `src/types.ts` containing comprehensive type definitions for the Wampy class
- Types defined:
  - `WampMessageType` union and `WampMessage` tuple type for WAMP message IDs
  - `WampyOpStatus` interface for operation status
  - `EventData`, `CallResult`, `InvocationData` callback data types
  - `EventCallback`, `RPCCallback`, `OnChallengeCallback`, `AuthPlugin` function types
  - `SubscribeSuccessResult`, `UnsubscribeSuccessResult`, `PublishSuccessResult`, `RegisterSuccessResult`, `UnregisterSuccessResult` result types
  - `Payload`, `PayloadWithArgsKwargs` for user-supplied payloads
  - `PackPPTPayloadResult`, `UnpackPPTPayloadResult` for PPT mode
  - `SubscribeAdvancedOptions`, `PublishAdvancedOptions`, `CallAdvancedOptions`, `CancelAdvancedOptions`, `RegisterAdvancedOptions` for public API
  - `ProgressiveCallSendData`, `ProgressiveCallSendDataOptions`, `ProgressiveCallReturn` for progressive calls
  - `WampyOptions` interface with all constructor/config options
  - `WampyCache` for internal cache/state (sessionId, reqId, timers, server features, etc.)
  - `ServerWampFeatures`, `ServerWampRole`, `WampFeatures` for WAMP features structures
  - `SubscriptionCallbacksHash`, `RegistrationCallbacksHash` for internal subscription/registration storage
  - `SubscribeRequestCallbacks`, `RegisterRequestCallbacks`, `WampRequest`, `WampCall` for pending requests
  - `TopicType`, `WampRole` for internal method parameters
  - `WebSocketConstructor`, `InvocationResult`, `InvocationResultOptions`, `InvocationErrorData` for RPC invocation handling
- Files changed:
  - Created: `src/types.ts`
  - Modified: `.ralph/prd.json`, `.ralph/progress.txt`
- **Learnings for future iterations:**
  - The `_requests` map is polymorphic — subscribe requests add `onEvent`, register requests add `rpc` callback, publish/unsubscribe requests use plain Deferred
  - The `_calls` map extends `Deferred<CallResult>` with optional `onProgress` for progressive call results
  - `WampyCache.sessionId` is `number | null` (received as a number from WELCOME message)
  - `ServerWampFeatures.roles` is `Record<string, ServerWampRole>` to allow indexing by dynamic role names ('dealer', 'broker')
  - `WampyOptions.payloadSerializers` is `Record<string, Serializer>` for PPT mode serializer lookup by name
  - Types use `import type` for all imports to ensure they are erased at compile time
---

## 2026-03-15 23:00 (EET) - US-007
- Converted `src/wampy.js` → `src/wampy.ts` with full TypeScript typing
- All private properties typed with explicit TS types: `_url: string | null`, `_protocols: string[]`, `_wamp_features: WampFeatures`, `_cache: WampyCache`, `_ws: WebSocket | null`, `_wsQueue: (string | ArrayBuffer | Uint8Array | undefined)[]`, `_requests: Record<number, WampRequest>`, `_calls: Record<number, WampCall>`, `_subscriptionsById: Map<number, SubscriptionCallbacksHash>`, `_subscriptionsByKey: Map<string, SubscriptionCallbacksHash>`, `_rpcRegs: Record<string | number, RegistrationCallbacksHash>`, `_rpcNames: Set<string>`, `_options: Required<WampyOptions>`
- Constructor overloads typed: `()`, `(url: string)`, `(options: WampyOptions)`, `(url: string, options: WampyOptions)`
- All private methods from constructor through `_wsOnOpen` fully typed with parameter and return types
- All remaining private methods (`_wsOnClose` through `_getSubscriptionKey`) and all public API methods also typed with parameter and return types to satisfy typecheck
- JSDoc `@param`, `@returns`, `@type`, `@private` annotations removed from converted methods; textual descriptions preserved as inline comments
- Updated `tsup.config.ts` entry paths from `.js` to `.ts` for wampy
- Files changed:
  - Created: `src/wampy.ts`
  - Deleted: `src/wampy.js`
  - Modified: `tsup.config.ts` (wampy entry paths updated), `.ralph/prd.json`, `.ralph/progress.txt`
- **Learnings for future iterations:**
  - `Deferred<T>` has contravariant `onSuccess` — `Deferred<SpecificType>` is NOT assignable to `Deferred<unknown>`. Use `as unknown as Deferred` when storing in `_requests`
  - `SUCCESS` constant from `constants.ts` only has `{ code: 0, error: null }` — need to spread with `reqId: 0` for `WampyOpStatus` assignment
  - `Required<WampyOptions>` turns `ws: WebSocketConstructor | null` into required non-optional but still nullable. When passing to `getWebSocket()` whose config expects `ws?:` (undefined-optional), cast `this._options as Record<string, unknown>` to avoid null/undefined mismatch
  - `_isPlainObject` works well as a type guard (`input is Record<string, unknown>`) — narrows the type in conditional blocks, eliminating many explicit casts
  - The file is 1923 lines — even though the PRD splits it into US-007 (first half) and US-008 (second half), the entire file must typecheck, so all methods need at least basic types. US-008 should focus on JSDoc cleanup of the remaining methods
   - WAMP message handler dispatch table needs explicit `Record<number, () => void | Promise<void>>` type and message data needs tuple casts for destructuring
---

## 2026-03-15 23:30 (EET) - US-008
- Cleaned 59 JSDoc type annotations (`@param`, `@returns`, `@return`, `@type`, `@private`) from remaining private methods and all public API methods in `src/wampy.ts`
- Preserved `@deprecated` annotation (line 1247) and all textual descriptions
- Preserved `@see` inline references to WAMP spec
- Verified exports remain: `export default Wampy`, `export { Wampy }`, `export * as Errors from './errors.js'`
- Files changed:
  - Modified: `src/wampy.ts` (JSDoc cleanup only, no logic changes)
  - Modified: `.ralph/prd.json` (US-008 passes: true)
- **Learnings for future iterations:**
  - US-007 already typed all methods with parameter and return types — US-008 was purely JSDoc annotation removal
  - The file went from 1923 to 1832 lines (91 lines removed) — all removed lines were JSDoc annotations
  - When cleaning JSDoc: remove `@param {type}`, `@returns`, `@return`, `@type`, `@private`, `@public` — but keep `@deprecated`, `@see`, and plain text descriptions
  - Some JSDoc blocks had both textual descriptions AND annotations — needed to preserve the text while removing annotations (e.g., WAMP SPEC format strings)
---

## 2026-03-15 23:45 (EET) - US-009
- Converted `src/wampy-single-4-browser.js` → `src/wampy-single-4-browser.ts` with `declare global` type augmentation for `Wampy`
- Converted `src/wampy-all-4-browser.js` → `src/wampy-all-4-browser.ts` with `declare global` type augmentation for `JsonSerializer`, `MsgpackSerializer`, `CborSerializer`, `WampyCryptosign`
- Updated `tsup.config.ts` entry paths from `.js` to `.ts` for all 4 browser IIFE bundles (single, all, single.min, all.min)
- Files changed:
  - Created: `src/wampy-single-4-browser.ts`, `src/wampy-all-4-browser.ts`
  - Deleted: `src/wampy-single-4-browser.js`, `src/wampy-all-4-browser.js`
  - Modified: `tsup.config.ts` (browser entry paths updated)
- **Learnings for future iterations:**
  - For `declare global` type augmentation of classes, use `typeof import('./module.js').ClassName` to reference the class constructor type
  - For namespace imports (`import * as foo from ...`), the global var type is `typeof import('./module.js')` (the whole module namespace)
  - Each `var` declaration in `declare global` needs its own `// eslint-disable-next-line no-var` comment
  - Git detects `.js` → `.ts` renames correctly when content is similar enough
---

## 2026-03-15 24:00 (EET) - US-010
- Converted core CLI files to TypeScript:
  - `cmd/cli.js` → `cmd/cli.ts` with shebang preserved
  - `cmd/main.js` → `cmd/main.ts` with yargs setup typed via `Argv` type from `@types/yargs`
  - `cmd/common-options.js` → `cmd/common-options.ts` with `Argv` parameter types, middleware callbacks returning `void`
  - `cmd/logger.js` → `cmd/logger.ts` with rest params typed as `...args: unknown[]`
  - `cmd/wampy-helpers.js` → `cmd/wampy-helpers.ts` with `CliArgv` and `PPTOptions` interfaces, `prepareOptions()` returning `WampyOptions`, `getWampySession()` returning `Promise<Wampy>`
- Added `@types/yargs` and `@types/ws` as devDependencies
- JSDoc type annotations removed; textual descriptions preserved
- Files changed:
  - Created: `cmd/cli.ts`, `cmd/main.ts`, `cmd/common-options.ts`, `cmd/logger.ts`, `cmd/wampy-helpers.ts`
  - Deleted: `cmd/cli.js`, `cmd/main.js`, `cmd/common-options.js`, `cmd/logger.js`, `cmd/wampy-helpers.js`
  - Modified: `package.json` (added @types/yargs, @types/ws devDeps), `package-lock.json`
- **Learnings for future iterations:**
  - yargs `MiddlewareFunction` expects `void` return — the original JS code returned `argv` but TypeScript requires `void`
  - `completion('completion', false)` — second arg `false` is valid (hides the command), types support `string | false`
  - Auth plugin `sign()` functions return `(method, info: SpecificType) => string` which is contravariant with `AuthPlugin = (method, extra: Record<string, unknown>) => string` — need `as unknown as AuthPlugin` cast
  - CLI files are NOT built by tsup (they run from source via Node.js ESM) — no tsup.config.ts changes needed
  - `ws` constructor type from `@types/ws` works with `WampyOptions.ws` via `as unknown as WampyOptions['ws']` cast since ws class signature differs slightly
  - The original JS code passed class constructors (not instances) to `payloadSerializers` — corrected to use `new Serializer()` instances for type safety and runtime correctness
---

## 2026-03-15 24:30 (EET) - US-011
- Converted CLI command files to TypeScript:
  - `cmd/commands/call.js` → `cmd/commands/call.ts` with `CallArgv` interface extending `CliArgv`, handler typed with `Arguments` from yargs + internal cast
  - `cmd/commands/publish.js` → `cmd/commands/publish.ts` with `PublishArgv` interface, all publish-specific options typed
  - `cmd/commands/subscribe.js` → `cmd/commands/subscribe.ts` with `SubscribeArgv` interface, match option typed as union literal
  - `cmd/commands/register.js` → `cmd/commands/register.ts` with `RegisterArgv` interface, match/invoke options typed as union literals
- Added index signature `[key: string]: unknown` to `PPTOptions` in `wampy-helpers.ts` for compatibility with `CallAdvancedOptions`/`PublishAdvancedOptions`
- JSDoc type annotations removed; textual descriptions preserved
- Files changed:
  - Created: `cmd/commands/call.ts`, `cmd/commands/publish.ts`, `cmd/commands/subscribe.ts`, `cmd/commands/register.ts`
  - Deleted: `cmd/commands/call.js`, `cmd/commands/publish.js`, `cmd/commands/subscribe.js`, `cmd/commands/register.js`
  - Modified: `cmd/wampy-helpers.ts` (added index signature to PPTOptions)
- **Learnings for future iterations:**
  - yargs `CommandModule` handler expects `(args: ArgumentsCamelCase<{}>) => void | Promise<void>` — a handler typed with a specific argv interface is incompatible. Use `Arguments` parameter type with internal `as unknown as SpecificArgv` cast
  - `PPTOptions` without an index signature is not assignable to types with `[key: string]: unknown` (like `CallAdvancedOptions`) — add the index signature to bridge the gap
  - CLI command files follow a consistent pattern: `command`, `description`, optional `aliases`, `builder` function (configures yargs options), `handler` async function, default export object
---

## 2026-03-15 25:00 (EET) - US-012
- Converted test helper/mock files to TypeScript:
  - `test/send-data.js` → `test/send-data.ts` with `SendDataItem` interface exported, `sendData` array typed as `SendDataItem[]`
  - `test/fake-ws.js` → `test/fake-ws.ts` with `FakeWebSocket` interface, `FakeWebSocketConstructor` interface, typed serializer map, typed timers, typed `send()` method with `as number[]` casts for `WAMP_MSG_SPEC` `.includes()` calls
  - `test/fake-ws-set-protocol.js` → `test/fake-ws-set-protocol.ts` with `FakeWebSocket` interface, typed constructor and prototype methods
  - `test/fake-wampy-mock.js` → `test/fake-wampy-mock.ts` with `FakeWampyOptions` interface, all methods typed with parameter and return types
- Added `@types/mocha`, `@types/chai`, `@types/sinon` as devDependencies
- Files changed:
  - Created: `test/send-data.ts`, `test/fake-ws.ts`, `test/fake-ws-set-protocol.ts`, `test/fake-wampy-mock.ts`
  - Deleted: `test/send-data.js`, `test/fake-ws.js`, `test/fake-ws-set-protocol.js`, `test/fake-wampy-mock.js`
  - Modified: `package.json` (added @types/mocha, @types/chai, @types/sinon), `package-lock.json`
- **Learnings for future iterations:**
  - Function-constructor pattern (`const Foo = function() { ... }`) needs `FakeWebSocket` interface + `as unknown as FakeWebSocketConstructor` cast to type correctly
  - `WAMP_MSG_SPEC` values are literal types due to `as const` — `.includes()` with `unknown` values requires casting the array to `number[]`
  - `send-data.ts` is 3500+ lines but only needs a single `SendDataItem` interface — all entries are data objects with the same shape
  - `structuredClone()` returns `SendDataItem` when input is `SendDataItem` — TypeScript infers this correctly
  - Test helper files only import from `../src/` (serializers, constants) — no circular dependency concerns
---

## 2026-03-15 26:00 (EET) - US-013
- Converted 7 small test files from JavaScript to TypeScript:
  - `test/wampy-utils-test.js` → `test/wampy-utils-test.ts` (226 lines) — `getWebSocket` return typed via `SimpleWebSocket` interface, `globalThis` mock via `Record<string, unknown>` alias
  - `test/wampy-cra-test.js` → `test/wampy-cra-test.ts` (77 lines) — `challengeInfo` typed with optional fields, error caught as `Error` cast
  - `test/wampy-cryptosign-test.js` → `test/wampy-cryptosign-test.ts` (55 lines) — `chai-bytes` bundled types work, empty object cast `as { challenge: string }`
  - `test/wampy-crossbar-test.js` → `test/wampy-crossbar-test.ts` (44 lines) — `websocket.w3cwebsocket` cast via `as unknown as WampyOptions['ws']`, `result.argsList!` non-null assertion
  - `test/wampy-browser-wrappers-test.js` → `test/wampy-browser-wrappers-test.ts` (31 lines) — `globalThis as Record<string, unknown>` for dynamic property access
  - `test/wampy-serializer-handshake-test.js` → `test/wampy-serializer-handshake-test.ts` (49 lines) — `WebSocket` cast `as unknown as WampyOptions['ws']`
  - `test/wampy-cli-help-test.js` → `test/wampy-cli-help-test.ts` (52 lines) — `runCommand` typed with `...args: string[]` and `Promise<string>` return
- Added `@types/websocket` as devDependency for crossbar test
- No logic changes — just enough typing to compile under strict: true
- Files changed:
  - Created: `test/wampy-utils-test.ts`, `test/wampy-cra-test.ts`, `test/wampy-cryptosign-test.ts`, `test/wampy-crossbar-test.ts`, `test/wampy-browser-wrappers-test.ts`, `test/wampy-serializer-handshake-test.ts`, `test/wampy-cli-help-test.ts`
  - Deleted: `test/wampy-utils-test.js`, `test/wampy-cra-test.js`, `test/wampy-cryptosign-test.js`, `test/wampy-crossbar-test.js`, `test/wampy-browser-wrappers-test.js`, `test/wampy-serializer-handshake-test.js`, `test/wampy-cli-help-test.js`
  - Modified: `package.json` (added @types/websocket devDep), `package-lock.json`
- **Learnings for future iterations:**
  - `globalThis.location` has a built-in DOM `Location` type — can't redeclare with partial mock. Use `const g = globalThis as Record<string, unknown>` and access `g.location` instead
  - `globalThis = savedGlobalThis` is illegal in strict TS (globalThis is a namespace) — needs `@ts-expect-error` suppression
  - `websocket` npm package needs `@types/websocket` for TypeScript; `w3cwebsocket` constructor is typed there
  - `chai-bytes` ships its own `index.d.ts` — no separate `@types` package needed
  - For test files that access dynamic globalThis properties (Wampy, MsgpackSerializer, etc.), cast globalThis to `Record<string, unknown>` rather than augmenting global declarations
  - `wampyCra.sign()` returns a function that is generic over method/info — call signatures match test usage without extra casts
  - Crossbar test uses `Partial<WampyOptions>` for option subsets, then casts `as WampyOptions` when constructing Wampy
- For tests that intentionally pass wrong types to methods (validation tests), use `as unknown as ExpectedType` casts to bypass TS compile-time checks while preserving runtime behavior
- Catch blocks accessing `WampError` properties (`errorUri`, `argsList`, `argsDict`) need `const err = e as Errors.WampError;` cast since `catch (e)` gives `unknown`
- `setOptions()` returns `Wampy | undefined` — chain calls need `!` (e.g., `wampy.setOptions({...})!.connect()`)
- `getOpStatus().error` is `Error | null` — accessing `.message` needs `!` assertion: `getOpStatus().error!.message`
- `getSessionId()` returns `number | null` — use `!` when assigning to `number` variable
- For test option objects with `uriValidation: 'loose'`, add `as const` to prevent widening to `string`
- The `wampy-cli-test.ts` replaces import in `cmd/wampy-helpers.ts` (not `.js`) at runtime for mocking
- yargs `parse()` callback type: `(err: Error | undefined, argv: Record<string, unknown>, output: string) => void`
- Serializer constructors in tests typed via `SerializerConstructor` interface: `{ new (): Serializer }`
- FakeWebSocket from `fake-ws.ts` needs `as unknown as WampyOptions['ws']` cast when used in test serializer array
---

## 2026-03-15 27:30 (EET) - US-014
- Converted 2 large test files from JavaScript to TypeScript:
  - `test/wampy-common-test.js` → `test/wampy-common-test.ts` (3456 lines) — extensive typing for validation tests using `as unknown as X` casts, `WampError` catch casts, `!` non-null assertions for `getOpStatus().error`, `res.argsList`, `e.argsList`; `SerializerConstructor` interface for generic serializer loop; `UserException1`/`UserException2` converted from function constructors to class syntax; circular reference objects typed with `Record<string, unknown>`
  - `test/wampy-cli-test.js` → `test/wampy-cli-test.ts` (187 lines) — `consoleLogSpy` typed as `sinon.SinonSpy`, `getArgvInstance()` returns `Promise<Argv>`, parse callbacks typed with `(err: Error | undefined, argv: Record<string, unknown>, output: string)`, `replaceFileName` updated to `.ts`
- No logic changes — tests remain functionally identical
- Files changed:
  - Created: `test/wampy-common-test.ts`, `test/wampy-cli-test.ts`
  - Deleted: `test/wampy-common-test.js`, `test/wampy-cli-test.js`
  - Modified: `.ralph/prd.json`, `.ralph/progress.txt`
- **Learnings for future iterations:**
  - Tests that intentionally pass wrong types for validation need extensive `as unknown as X` casts — roughly 30 casts needed in wampy-common-test alone
  - `setOptions()` returns `Wampy | undefined` — all chained calls need `!` assertion (37 instances in common test)
  - `catch (e)` in TypeScript gives `unknown` type — any property access beyond `expect(e).to.be.instanceOf()` needs explicit cast (`const err = e as Errors.WampError`)
  - Git correctly detects `.js` → `.ts` renames even with ~17% content change (casts + type annotations)
  - `PublishAdvancedOptions` and `CallAdvancedOptions` have `[key: string]: unknown` index signatures — passing wrong-typed fields within objects still requires `as unknown as Type` cast because TypeScript checks known properties first
  - `onConnect` is NOT a valid WampyOptions property — tests using it need `as WampyOptions` cast on the option object
  - CLI test's file replacement hack needs `.ts` extension now that CLI files are TypeScript
---

## 2026-03-15 21:30 (EET) - US-015
- Updated test runner and coverage configuration for TypeScript
- Replaced `@babel/register` mocha require hook with `tsx` via `NODE_OPTIONS='--import tsx/esm'`
- Updated all test script glob patterns from `.js` to `.ts` in package.json
- Updated `.c8rc.json` coverage include patterns from `*.js` to `*.ts`
- Replaced `karma-browserify` + `babelify` with `karma-esbuild` in `karma.conf.cjs`
- Added `tsx` and `karma-esbuild` as devDependencies
- Files changed:
  - Modified: `package.json` (test scripts updated, tsx/karma-esbuild added as devDeps)
  - Modified: `.c8rc.json` (include patterns updated)
  - Modified: `karma.conf.cjs` (switched from browserify+babelify to esbuild)
  - Modified: `package-lock.json` (dependency tree updated)
- Test results:
  - `npm run test:node-no-browser-wrappers`: 528 passing (2 crossbar failures expected — no server running)
  - `npm run test:browser-wrappers`: 4 passing
  - `npm run test:browser` (Karma): 487 passing
  - Coverage: 84.61% statements, 93.82% branches, 96.29% functions
- **Learnings for future iterations:**
  - Mocha does NOT have a `--import` flag — use `NODE_OPTIONS='--import tsx/esm'` via `cross-env` to register the tsx ESM loader
  - `--import tsx` without `/esm` fails with ERR_MODULE_NOT_FOUND — always use `tsx/esm` for ESM projects
  - `karma-esbuild` handles TypeScript natively with zero config — just set preprocessor pattern to `'test/**/*.ts': ['esbuild']`
  - esbuild in karma config needs `format: 'esm'`, `bundle: true`, `platform: 'browser'` for correct browser bundle output
  - Crossbar tests always fail without a running Crossbar.io router — these are integration tests, not unit tests
---

## 2026-03-15 28:00 (EET) - US-016
- Updated package.json exports and module configuration for proper ESM/CJS/types distribution
- Changes made:
  - `main` → `dist/cjs/wampy.cjs` (CJS entry)
  - `module` → `dist/esm/wampy.js` (ESM entry)
  - `types` → `dist/esm/wampy.d.ts` (declaration entry)
  - `exports` map updated with `types`, `import`, `require` conditions for all 6 entry points (`.`, `./JsonSerializer.js`, `./CborSerializer.js`, `./MsgpackSerializer.js`, `./cryptosign.js`, `./wampcra.js`)
  - `bin` → `dist/cli/cli.js` (compiled CLI output instead of source `.ts`)
  - `files` field added: `dist/esm/`, `dist/cjs/`, `dist/browser/`, `dist/browser.zip`, `dist/cli/`
  - `type: "module"` retained
  - CLI build added to `tsup.config.ts` as ESM bundle with external yargs deps
  - `yargs/helpers` and `yargs/yargs` added to commonExternal in tsup config
- Files changed:
  - Modified: `package.json` (main, module, types, exports, bin, files)
  - Modified: `tsup.config.ts` (CLI build config added, yargs subpath externals)
- **Learnings for future iterations:**
  - esbuild/tsup preserves shebang from source files automatically — do NOT add `banner: { js: '#!/usr/bin/env node' }` or the shebang gets duplicated
  - `yargs/helpers` and `yargs/yargs` are separate entry points that must be externalized individually (not covered by just `yargs`)
  - `exports` map must list `types` condition first — TypeScript resolvers check conditions in order and stop at the first match
  - `wampcra.js` CJS `require` condition points to `.mjs` because wampy-cra uses top-level await (incompatible with CJS)
  - The `files` field controls what gets published to npm — must include all dist subdirectories but NOT source files
---

## 2026-03-15 29:00 (EET) - US-017
- Updated ESLint configuration to lint TypeScript files with typescript-eslint
- Changes made:
  - Installed `typescript-eslint` v8 as devDependency
  - Rewrote `eslint.config.js` to use `tseslint.config()` wrapper with flat config
  - File patterns changed from `.js` to `.ts` for src/, cmd/, test/; kept `.cjs`/`.js` for karma.conf.cjs and eslint.config.js
  - Added `tseslint.configs.recommended` for TS-specific rules
  - Replaced JS rules with TS equivalents: `no-unused-vars` → `@typescript-eslint/no-unused-vars`, `no-use-before-define` → `@typescript-eslint/no-use-before-define`, `default-param-last` → `@typescript-eslint/default-param-last`
  - Disabled `no-duplicate-imports` for TS files (import type + import from same module)
  - Disabled `@typescript-eslint/no-unused-expressions` for test files (chai assertions)
  - Disabled `@typescript-eslint/no-this-alias` (consistent-this pattern)
  - Disabled `@typescript-eslint/no-explicit-any`, `@typescript-eslint/no-require-imports`, `@typescript-eslint/no-empty-object-type` for codebase compatibility
  - Added `unicorn/prefer-class-fields: off` (serializer constructor patterns)
- Fixed lint issues in source files:
  - `cmd/wampy-helpers.ts`: Added `{ cause: error }` to re-thrown Error (unicorn/preserve-caught-error)
  - `src/wampy.ts`: Removed unused `InvocationResultOptions` import
  - `test/wampy-utils-test.ts`: Added `eslint-disable-next-line no-global-assign` for globalThis reassignment
  - Removed 8 stale `eslint-disable no-var` directives from browser wrapper files and utils.ts (no longer needed with TS `declare global`)
  - Removed stale `eslint-disable no-unused-vars` directive from cmd/cli.ts
- Files changed:
  - Modified: `eslint.config.js` (rewritten for TypeScript support)
  - Modified: `package.json` (added typescript-eslint devDep)
  - Modified: `package-lock.json` (dependency tree updated)
  - Modified: `cmd/wampy-helpers.ts` (error cause added)
  - Modified: `cmd/cli.ts` (stale directive removed)
  - Modified: `src/wampy.ts` (unused import removed)
  - Modified: `src/utils.ts` (stale directive removed)
  - Modified: `src/wampy-single-4-browser.ts` (stale directive removed)
  - Modified: `src/wampy-all-4-browser.ts` (stale directives removed)
  - Modified: `test/wampy-utils-test.ts` (eslint-disable added for globalThis)
- **Learnings for future iterations:**
  - `typescript-eslint` v8 uses `tseslint.config()` wrapper for flat config — replaces manual parser/plugin setup
  - TS overrides must come AFTER base config blocks — flat config applies rules in declaration order, last wins
  - `no-duplicate-imports` doesn't understand `import type` — must be disabled for TS files
  - `@typescript-eslint/no-unused-expressions` triggers on chai assertion chains like `expect(x).to.be.true` — disable for test files
  - `unicorn/preserve-caught-error` (new rule) requires `{ cause }` when re-throwing errors — good practice regardless
  - `eslint --fix` auto-removes unused eslint-disable directives
  - `dist/*` should be in ignores to avoid linting build output
---

## 2026-03-15 30:00 (EET) - US-018
- Updated GitHub Actions workflow `.github/workflows/build-and-test.yml` for TypeScript codebase
- Changes made:
  - Node.js matrix updated from `[18, 20, 22]` to `[20, 22]` (Node 18 dropped)
  - Added explicit `npm run build` step (tsup) before tests
  - Added `npm run typecheck` step to catch type errors in CI
  - Added `npm run lint` step to enforce code quality in CI
  - `npm test` continues to run all test suites (node + browser-wrappers + karma browser)
  - Coverage report step changed from `./node_modules/c8/bin/c8.js report` to `npx c8 report`
  - `coverallsapp/github-action@master` updated to `coverallsapp/github-action@v2`
  - Added `coveralls_finish` job for parallel coverage upload completion
- Files changed:
  - Modified: `.github/workflows/build-and-test.yml`
  - Modified: `.ralph/prd.json` (US-018 passes: true)
- **Learnings for future iterations:**
  - `coverallsapp/github-action@v2` is the current stable version — `@master` is deprecated
  - When using `parallel: true` for Coveralls with matrix builds, a separate `coveralls_finish` job with `parallel-finished: true` is required to signal completion
  - CI pipeline order should be: install → build → typecheck → lint → test → coverage report → upload
  - `npx c8 report` is preferred over direct `./node_modules/c8/bin/c8.js` path for portability
---

## 2026-03-15 21:45 (EET) - US-019
- Cleaned up removed files and updated documentation for TypeScript migration
- Verified all .js source/test/cmd files already removed in prior stories (US-001 through US-014)
- Verified Gruntfile.cjs and .babelrc already removed in US-001
- Verified karma.conf.cjs already updated to use esbuild in US-015
- Removed obsolete `karma-browserify` devDependency (replaced by `karma-esbuild` in US-015 but never uninstalled)
- Updated README.md:
  - Description: "javascript library" → "TypeScript library", added note about built-in `.d.ts` types
  - Usage example: changed from `javascript` to `typescript` code block with `import` statement
  - Exported components: updated exports map with correct `dist/esm/`, `dist/cjs/` paths and `types` conditions
  - Browser installation: removed `browser/` path prefix (browser.zip has files at root)
  - API section: replaced DefinitelyTyped reference with "ships with built-in TypeScript type declarations"
  - TLS example: changed from `require()` to ESM `import` syntax
  - Tests section: updated with current scripts (`npm run build`, `npm run typecheck`, `npm run lint`)
  - File references: `src/errors.js` → `src/errors.ts`, serializer files → `.ts` extensions
  - Custom serializer section: added note about `Serializer` interface in `src/serializers/serializer.ts`
- Updated CONTRIBUTING.md:
  - Project Setup: added `npm run build`, `npm run typecheck`, `npm run lint` instructions, Node.js v20/v22 recommendation
  - Contribute Code: added typecheck and lint requirements
  - CI reference: "Travis, AppVeyor" → "GitHub Actions CI"
- .gitignore: no changes needed (already has `dist/` covering new output structure)
- Files changed:
  - Modified: `README.md` (TypeScript migration documentation updates)
  - Modified: `CONTRIBUTING.md` (new build/test instructions)
  - Modified: `package.json` (removed karma-browserify devDep)
  - Modified: `package-lock.json` (dependency tree updated)
  - Modified: `.ralph/prd.json` (US-019 passes: true)
- **Learnings for future iterations:**
  - Previous stories already handled all file renames/deletions — this story was primarily documentation cleanup
  - `karma-browserify` was replaced by `karma-esbuild` in US-015 but was never removed from devDependencies — always check for leftover deps when switching tools
  - README exports map must be kept in sync with package.json exports — they can easily drift
  - Browser bundles in `browser.zip` are at the root level (not in a `browser/` subdirectory) — update `<script src>` examples accordingly
---
